CREATE TABLE RESERVED_WORD (
  ID INT NOT NULL AUTO_INCREMENT,
  WORD INT NOT NULL,
  CONSTRAINT PK_Reserved_Word_ID PRIMARY KEY (ID)
  CONSTRAINT FK_RESERVED_WORD_WORD FOREIGN KEY ( WORD ) REFERENCES WORD ( ID )
);
