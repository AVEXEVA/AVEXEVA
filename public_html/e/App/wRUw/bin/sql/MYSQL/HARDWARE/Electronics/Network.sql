CREATE TABLE Network (
  ID INT NOT NULL AUTO_INCREMENT,
  Router INT,
);
CREATE TABLE IP (
  ID INT NOT NULL AUTO_INCREMENT,
  Address VARCHAR(256)
  CONSTRAINT PK_IP_ID PRIMARY KEY (ID)
);
CREATE TABLE Port (
  ID INT NOT NULL,
  Number INT NOT NULL
);
